<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Check Your Email - SanctumTools</title>
  <link rel="stylesheet" href="/css/design-system.css">
  <style>
    .auth-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, var(--color-background) 0%, #f0e8dd 100%);
      padding: var(--spacing-lg);
    }

    .auth-card {
      background: var(--color-white);
      border-radius: var(--radius-2xl);
      padding: var(--spacing-2xl);
      max-width: 540px;
      width: 100%;
      box-shadow: 0 8px 32px rgba(153, 50, 168, 0.15);
      text-align: center;
    }

    .success-icon {
      width: 80px;
      height: 80px;
      margin: 0 auto var(--spacing-lg);
      background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
      border-radius: var(--radius-full);
      display: flex;
      align-items: center;
      justify-content: center;
      animation: scale-in 0.5s ease-out;
    }

    .success-icon svg {
      width: 48px;
      height: 48px;
      color: var(--color-white);
    }

    @keyframes scale-in {
      from {
        transform: scale(0);
        opacity: 0;
      }
      to {
        transform: scale(1);
        opacity: 1;
      }
    }

    .confirmation-title {
      font-size: var(--font-size-2xl);
      font-weight: var(--font-weight-bold);
      color: var(--color-text-primary);
      margin-bottom: var(--spacing-md);
    }

    .confirmation-text {
      color: var(--color-text-secondary);
      font-size: var(--font-size-base);
      line-height: var(--line-height-relaxed);
      margin-bottom: var(--spacing-lg);
    }

    .email-display {
      display: inline-block;
      background: var(--color-gray-100);
      padding: var(--spacing-xs) var(--spacing-md);
      border-radius: var(--radius-lg);
      font-weight: var(--font-weight-medium);
      color: var(--color-primary);
      margin: var(--spacing-sm) 0;
    }

    .info-box {
      background: var(--color-gray-50);
      border-left: 4px solid var(--color-primary);
      padding: var(--spacing-lg);
      border-radius: var(--radius-lg);
      margin: var(--spacing-xl) 0;
      text-align: left;
    }

    .info-box h3 {
      font-size: var(--font-size-base);
      font-weight: var(--font-weight-semibold);
      color: var(--color-text-primary);
      margin: 0 0 var(--spacing-sm) 0;
    }

    .info-box ul {
      margin: 0;
      padding-left: var(--spacing-lg);
      color: var(--color-text-secondary);
      font-size: var(--font-size-sm);
    }

    .info-box li {
      margin: var(--spacing-xs) 0;
    }

    .help-text {
      color: var(--color-text-muted);
      font-size: var(--font-size-sm);
      margin-top: var(--spacing-xl);
      padding-top: var(--spacing-lg);
      border-top: 1px solid var(--color-gray-200);
    }

    .help-text a {
      color: var(--color-primary);
      text-decoration: none;
      font-weight: var(--font-weight-medium);
    }

    .help-text a:hover {
      text-decoration: underline;
    }

    @media (max-width: 480px) {
      .auth-card {
        padding: var(--spacing-lg);
      }

      .confirmation-title {
        font-size: var(--font-size-xl);
      }
    }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="auth-card animate-fade-in">
      <div class="success-icon">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
      </div>

      <h1 class="confirmation-title">Check your email</h1>

      <p class="confirmation-text">
        We've sent a verification email to:
      </p>

      <div class="email-display"><%= email %></div>

      <p class="confirmation-text">
        Click the verification link in the email to activate your account and start using SanctumTools.
      </p>

      <div class="info-box">
        <h3>What to do next:</h3>
        <ul>
          <li>Check your inbox for an email from SanctumTools</li>
          <li>Click the verification link in the email</li>
          <li>If you don't see it, check your spam or junk folder</li>
          <li>The verification link expires in 24 hours</li>
        </ul>
      </div>

      <div class="help-text">
        <p>Didn't receive the email? <a href="/signup">Try signing up again</a></p>
        <p>Need help? <a href="mailto:support@sanctumtools.com">Contact support</a></p>
      </div>
    </div>
  </div>
</body>
</html>
