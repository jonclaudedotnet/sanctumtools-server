<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - SanctumTools</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #F5F1E8 0%, #f0e8dd 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .auth-card {
      background: white;
      border-radius: 24px;
      padding: 50px;
      max-width: 480px;
      width: 100%;
      box-shadow: 0 8px 32px rgba(153, 50, 168, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.8);
    }

    h2 {
      font-size: 26px;
      font-weight: 700;
      text-align: center;
      margin-bottom: 25px;
      color: #4a4a4a;
    }

    .form-group {
      margin-bottom: 24px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-weight: 700;
      color: #4a4a4a;
      font-size: 14px;
    }

    input {
      width: 100%;
      padding: 14px 16px;
      border: 2px solid #e8e1d5;
      border-radius: 12px;
      font-size: 16px;
      color: #333;
      background: #ffffff;
      transition: border-color 0.2s;
    }

    input:focus {
      outline: none;
      border-color: #9932a8;
      box-shadow: 0 0 0 3px rgba(153, 50, 168, 0.1);
    }

    button {
      width: 100%;
      padding: 14px 24px;
      font-size: 16px;
      font-weight: 600;
      border: none;
      border-radius: 12px;
      background: linear-gradient(135deg, #9932a8 0%, #B565C7 100%);
      color: white;
      cursor: pointer;
      transition: all 0.2s;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(153, 50, 168, 0.3);
    }

    .error-message {
      background: #fee;
      border: 1px solid #fcc;
      color: #c00;
      padding: 12px 16px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
    }

    .switch-mode {
      text-align: center;
      margin-top: 20px;
      font-size: 14px;
      color: #6a6a6a;
    }

    .switch-mode a {
      color: #9932a8;
      text-decoration: none;
      font-weight: 600;
    }

    .switch-mode a:hover {
      text-decoration: underline;
    }

    .back-link {
      text-align: center;
      margin-bottom: 20px;
    }

    .back-link a {
      color: #9932a8;
      text-decoration: none;
      font-size: 14px;
    }

    .back-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="auth-card">
    <div class="back-link">
      <a href="/">← Back</a>
    </div>

    <h2>Login</h2>

    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>

    <form method="POST">
      <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" placeholder="your@email.com" required autofocus>
      </div>

      <div class="form-group">
        <label for="code">Authenticator Code: <span style="color: #666; font-weight: normal;">(optional if device is remembered)</span></label>
        <input type="text" id="code" name="code" placeholder="000000 - Leave empty if this device is remembered" maxlength="6" pattern="[0-9]*">
      </div>

      <div style="background: #f0f5f9; border-left: 4px solid #9932a8; padding: 12px 14px; border-radius: 6px; margin-bottom: 20px; font-size: 13px; color: #555;">
        <strong>💡 Pro tip:</strong> This device will be remembered after your first successful login. You won't need to enter the code again for 30 days.
      </div>

      <button type="submit">Login</button>
    </form>

    <p class="switch-mode">
      Don't have an account? <a href="/signup">Sign up</a>
    </p>
  </div>
</body>
</html>
